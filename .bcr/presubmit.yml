bcr_test_module:
  module_path: "e2e/smoke"
  matrix:
    platform: ["debian10", "macos_arm64", "ubuntu2004", "windows"]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      test_targets:
        - "//..."
